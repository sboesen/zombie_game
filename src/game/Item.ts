export class Item {
    constructor(
        public name: string,
        public description: string,
        public type: string,
        public effect: number,
        public quantity: number
    ) {}
}